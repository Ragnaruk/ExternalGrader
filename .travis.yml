language: python

python:
  - "3.8-dev"

services:
  - docker

before_install:
  - make prepare

install:
  - make requirements-test

jobs:
  include:
    - stage: "Tests"
      name: "Unit Tests"
      script:
        - pytest --cov=./ -v
#    - name: "Integration Tests"
#      script:
#        - make compose

after_success:
  - codecov